export enum ProfileMode{
    DATING = 'dating',
    NETWORKING = 'networking',
    PROJECTS = 'projects'
}

export enum UserRole{
    FRONTEND = 'frontend',
    BACKEND = 'backend',
    FULLSTACK = 'fullstack',
    QA = 'qa',
    DEVOPS = 'devops',
    DATA = 'data',
}

export enum ExperienceLevel {
  STUDENT = 'student',
  JUNIOR = 'junior',
  MID = 'mid',
  SENIOR = 'senior',
  LEAD = 'lead',
}